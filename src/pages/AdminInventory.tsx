import { useEffect,useState } from "react";
import { Link } from "react-router-dom";
import { supabase } from "../services/supabase";
type Row={variant_id:string;quantity_on_hand:number;quantity_reserved:number;low_stock_threshold:number;track_inventory:boolean;product_variants:{name:string;color:string|null;sku:string;products:{name:string}|null}|null};
type Move={id:string;quantity_change:number;movement_type:string;reason:string|null;created_at:string;product_variants:{sku:string;products:{name:string}|null}|null};
export default function AdminInventory(){const [rows,setRows]=useState<Row[]>([]);const [moves,setMoves]=useState<Move[]>([]);const [error,setError]=useState("");useEffect(()=>{(async()=>{if(!supabase)return;const {data,error}=await supabase.from("inventory").select("variant_id,quantity_on_hand,quantity_reserved,low_stock_threshold,track_inventory,product_variants(name,color,sku,products(name))").order("updated_at",{ascending:false});if(error)setError(error.message);else setRows((data??[]) as unknown as Row[]);const {data:m}=await supabase.from("stock_movements").select("id,quantity_change,movement_type,reason,created_at,product_variants(sku,products(name))").order("created_at",{ascending:false}).limit(30);setMoves((m??[]) as unknown as Move[])})()},[]);return <main className="admin-content"><div className="admin-page-head"><div><p className="admin-kicker">MEIRO / ADMIN</p><h1>Нөөц</h1><p>Бүтээгдэхүүний нөөц болон хөдөлгөөнийг хянана.</p></div></div>{error&&<p className="admin-error">{error}</p>}<div className="admin-stock-list">{rows.map(r=>{const available=r.quantity_on_hand-r.quantity_reserved;const low=r.track_inventory&&available<=r.low_stock_threshold;return <article className="admin-stock-row" key={r.variant_id}><div><strong>{r.product_variants?.products?.name??"—"}</strong><small>{r.product_variants?.color||r.product_variants?.name} · {r.product_variants?.sku}</small></div><span>{r.track_inventory?(low?"Цөөн үлдсэн":"Бэлэн"):"Хянахгүй"}</span><b>{available}</b><Link to={"/admin/products/"+r.variant_id}>Засах</Link></article>})}</div><section className="admin-panel admin-stock-history"><h2>Сүүлийн хөдөлгөөн</h2>{moves.map(m=><div className="admin-movement" key={m.id}><div><strong>{m.product_variants?.products?.name??"—"}</strong><small>{m.product_variants?.sku} · {m.reason||m.movement_type}</small></div><b>{m.quantity_change>0?"+":""}{m.quantity_change}</b><time>{new Date(m.created_at).toLocaleDateString("mn-MN")}</time></div>)}</section></main>}
